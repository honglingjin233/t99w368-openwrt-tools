# modem5g 其他AI问题修复实现记录（2026-09-10）

> 依据：`其他ai分析/` 下 4 份报告（代码审查、方案审查、AI-HANDOFF、调研笔记）+ 本机核对
> 全部修复已实机部署 OpenFi6C（192.168.21.1）并验证。

## 一、确认结论

### 属实并已修复（本轮全部落地）
| 问题 | 对应分析 | 修复 |
|------|---------|------|
| uci device 写数值索引（漂移后 ifup 失配）| BUG-①/D-002 | 新增 `m5g-dev.sh` 解析 sysfs 物理路径；`syncDevicePath` 仅变化时写 flash；**实机验证：索引 2→3 漂移时 device 自动同步为 /sys/devices/... 并重连成功** |
| set_apn 密码单引号注入（rpcd=root）| A1 | 禁单引号 + 限长 63 + 带时间戳备份（保留 5 份）|
| 看门狗 60s 无 IP 即重拨 / 网关 ICMP 被禁时 74s 重拨风暴 | A10/BUG-⑧ | v4：连续 3 次失败 + 公网 DNS 二次确认 + 日志轮转 + MM 兜底重启 + hold 互斥 |
| 看门狗 4G 收信判断误匹配 supported 列表 | 自发现 | 改用 JSON current-modes 精确判断 `allowed: 4g;` |
| mmcli 扫描无显式超时（D-Bus 25s 自爆）| A2 | `--timeout=90` + hold + trap 清理 |
| rpcd 内 sleep(2) 阻塞 ubus 串行队列 | D1/A6 | 全量 execm 超时包装；speed 改读趋势缓存 |
| SIM 编号与 modem 编号分叉 | D-006/A7 | 用 `mmcli -K` 的 modem.generic.sim 真实路径 |
| 假连接/无 IP 分支只 ifup 不 ifdown | A3 | 先下后上 + 绑源 DNS 二次确认 |
| 信号轮询 3s 风暴 | A18 | signal-setup=30 |
| IPv6 显示链路本地地址 | A8 | 只取 scope global |
| 发送短信后 2s 盲切回 | A14/BUG-③ | 后端 auto_back 定时恢复原制式（页面关闭也生效）+ 发送状态轮询确认 |
| 收信模式硬编码恢复 3g\|4g\|5g | D4/A13 | set_mode 预记录原制式，恢复原制式（含 null 污染防护+回退默认）|
| USSD 假支持 | A20 | 前端置灰 + 文案标注 |
| 频段空串选项 | A21 | n 频段为空时禁选 |
| sms_list 无上限 | P2-8 | 上限 20 条 |
| CGCONTRDP/CGPADDR 白名单写死 cid=5 | P1-5 | 动态 [1-9] |
| 前端无 .catch / 固定 60s 硬 reload | BUG-④/A16 | 全按钮 .catch + 重拨/APN 客户端守护 40s + 复位轮询探测（15s×8）|
| 扫描无断网提示 | A17 | notify 提示 + hold 互斥 |
| AT 通道：adbd 重启未等就绪、pkill 自匹配自杀、锁残留 | 自发现 | 就绪轮询 + 锚定 pkill `^cat` + 锁持有者 PID 存活检测 |
| temp 正则 `TSENS: 45` 无 C 不匹配 | D5 | `TSENS:\s*(\d+)\s*C?` + 60s 缓存 |

### 误报 / 无需修复（已核对）
- BUG-② usage 正则：实际 7 组 `\s+\d+`，m[2] 正确取 tx_bytes（字段9）。**实机验证 rx/tx 数值正确。**
- B2 "proto 无 [ -e ] 检查"：属实但 sysfs 路径方案已覆盖根因。
- B3 rpc declare timeout：前端守护已覆盖，rpc 层不声明。
- B4 for-in 数组语义：ucode 标准行为，无问题。
- USSD CLI 保留；MCP/事件驱动等优化项不做（属可选增强）。

## 二、部署文件清单（本地权威版 = /home/hong/桌面/T99W368/modem5g-plugin/）
| 文件 | 版本变化 |
|------|---------|
| modem5g.uc | execm 超时/joinArr/syncDevicePath/注入防护/mode-before/短信轮询/sim_info -K/IPv6/cid 动态 |
| modem5g-status.js | .catch/守护/复位轮询/后端短信兜底/USSD 灰化/速率前端计算 |
| m5g-watchdog.sh | v4：失败计数+DNS 确认+hold+sysfs 绑定+日志轮转+MM 兜底 |
| m5g-at.sh | v2：就绪轮询+锚定 pkill+锁 PID 存活+白名单+缓存 |
| m5g-dev.sh | 新增：索引→sysfs 路径 |
| m5g-scan.sh / m5g-reset-restore.sh / m5g-sms-back.sh | 超时/hold/trap/恢复原制式 |
| modem5g-acl.json | scan 移至 write 组 |
| 安装部署说明.md | 文件清单补 m5g-dev.sh |

## 三、实机验证结果
- 索引漂移：部署期间 modem 2→3 漂移，旧配置 device=2 致 wwan0 起不来；v4 看门狗 3 次失败后自动同步 sysfs 路径并重连成功（16:41 日志"自动重拨(连续3次失败,原因:无IP)"）
- temp 44°C（缓存二次调用瞬时）、sim_info 正确、usage 正确、ATI/AT+temp? 全通
- 短信制式链路：切 4G → mode-before 记录 `3g|4g|5g` → auto_back(0) 恢复 → 制式回 3g|4g|5g ✓
- 绑源 ping 223.5.5.5 0% 丢包
- 看门狗单实例运行（pidof 与 pidfile 一致）

## 四、部署后发现的平台坑（已修复并入代码）
1. 本机 ucode（rpcd 内嵌）全局 `join()` 返回 null 不抛错 → 手写 joinArr（顺带修好 status.ports 一直为 null 的显示问题）
2. standalone `ucode` CLI 的 popen().read() 不可用（仅 rpcd 内可用）→ 语法校验用 `ucode -c`，运行时问题靠 ubus 实测
3. 路由无 stat/pkill/cksum → 文件时效用 `date +%s` 落文件、进程匹配用 kill -0 + /proc/cmdline

## 五、遗留/注意
- at_cmd 对无 OK 回执的指令（如 AT+CGSN 仅回 IMEI）返回 ok:false 但 output 正常，前端展示不受影响（历史行为）
- m5g-at-bridge/fwd.sh 为旧 AT 桥守护（当前未运行），AT 通道以 m5g-at.sh 为准
- 回滚：备份在 /root/backup/t99w368-20260910-0020/ 与路由 /tmp/m5g-bak-20260910-0028/
