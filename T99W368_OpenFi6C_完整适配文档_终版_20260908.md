# T99W368 5G 模块 × OpenFi6C 完整适配文档（终版）

**归档日期**: 2026-09-08
**状态**: ✅ 全链路打通并稳定运行（重启全自动恢复，USB3 接口）
**本文件整合自**: 2026-08-29 至 2026-09-08 全部 16 份历史报告/进度/逆向文档（已并入本文档后清理）

---

## 一、项目总览

| 项目 | 内容 |
|---|---|
| 目标 | 让 T99W368 5G 模块在 OpenFi6C 路由器上实现稳定 5G 上网 |
| 最终结论 | **模块无缺陷；根因 = OpenFi 原固件（ImmortalWrt 5.4 内核）的 USB 栈与模块工程固件 qbi 不兼容** |
| 最终解法 | 刷 **OpenWrt 官方 snapshot（内核 6.18.44）** + ModemManager 1.24 + libmbim 1.32 标准栈，全自动成功 |
| 当前状态 | 5G 注册（中国移动 CMCC / 5G NR），IPv4+IPv6 双栈拨号，重启后约 90 秒全自动恢复 |
| 模块位置 | USB3 口（SuperSpeed 5000 Mbps，xhci-mtk 2-1.2） |

**一句话因果链**：ImmortalWrt（5.4 内核）上模块无论用什么软件栈（libmbim 1.30/1.32、MM 1.22/1.24、USB2/3、冷启动）都被自身 qbi 层拒绝 MBIM OPEN（status=2）→ 换 OpenWrt 官方 6.18 内核后 qbi 交互正常 → 标准 ModemManager 栈全自动驱动成功。

---

## 二、硬件与环境

### 2.1 T99W368 模块
- **厂商**: Fibocom（工程版样片，Qualcomm 公版 VID:PID **05c6:90d5**）
- **平台**: Qualcomm Snapdragon X65（SDX65，型号标识 SDXLEMUR-SD-MTP _SN:1D0E9301）
- **固件**: FDE.F0.0.0.1.3.DF.004（工程版，非量产正式版）
- **IMEI**: 35518383******
- **模块内部**: 内嵌 Linux/Android 系统（可 ADB 进入）；关键进程 mbimd（MBIM 守护）、port_bridge、netmgrd、qmodem 等
- **USB gadget 配置**: `DIAG_ADB_MBIM_GNSS_DUN`（DIAG 调试口 + ADB + MBIM + GNSS + DUN）
- **USB 接口布局**: If#0 = QMI 控制、If#2 = MBIM（cdc_mbim 绑定，数据走 GSI DMA 管道直通 modem，**不依赖 IPA uC**）、If#5 = AT 口（class ff ff 40 厂商自定义类，无标准驱动）
- **SIM**: 单 nano-SIM（slot 1），本机移动卡（ICCID 89860068********），有 sim-pin2 锁（不阻塞注册）

### 2.2 OpenFi6C 路由器
- **平台**: MediaTek MT7981B（filogic），双核 A53
- **原厂固件**: 定制 ImmortalWrt 分支（openfi6c_new，内核 5.4.284）——**问题源头**
- **现固件**: OpenWrt 官方 snapshot（内核 6.18.44，aarch64_cortex-a53，apk 包管理）——**最终方案**
- **USB 拓扑**:
  - usb1 根（1d6b:0002, USB2）→ hub 1-1（05e3:0610）→ 模块原接 1-1.3（HighSpeed）
  - usb2 根（1d6b:0003, USB3.2 20Gbps）→ hub 2-1（05e3:0626, SuperSpeed）→ **模块现接 2-1.2（SuperSpeed 5000）**
- **网络现状**: LAN 192.168.21.1（有线口 enp1s0 对端）；WAN = wwan0（5G）；WiFi radio0/radio1 正常

### 2.3 对照参考机（本机）
- Debian 13 / x86_64 / 内核 6.12 / ModemManager 1.24 / libmbim 1.32 / USB2 直连
- 模块在本机**插上即用**，是"正常行为"的黄金参照

---

## 三、安全红线（最高优先级，必须永远遵守）

1. **绝不向 T99W368 的 DIAG/ADB tty 口（ttyUSB0/1）写任何数据**——曾触发模块 USB 保护性断连（hub 级，软件复位无效，只能整机断电重启恢复）
2. **禁止一切 USB 模式切换命令**（AT+USBSWITCH、setprop persist.sys.usb.config、usb_composition_switch 等，含经 ADB）——历史事故：AT+USBSWITCH=9013 致模块 USB 软锁死，物理拔插 M.2 无效，送修刷固件才恢复
3. **任何修改模块状态的命令（AT/ADB/MBIM 写）执行前必须停下说明用途、等用户批准**；只读查询可自动执行
4. 模块换接口/断电后，**MM 的 modem 编号可能变化**（如 Modem/0 → Modem/1），wwan 接口 device='0' 可能失配——重启 OpenFi（冷启动编号归零）是可靠复位手段

---

## 四、完整历程时间线（2026-08-29 → 09-08）

### 阶段一（08-29）：初识与 USB 锁死事故
- 模块接入 OpenFi（ImmortalWrt），识别为 05c6:90d5，但数据面 rx=0 完全不通
- 误发 AT+USBSWITCH=9013 致模块 USB 软锁死（送修恢复）；此后确立红线
- 通过 ADB 进入模块内部确认基本盘：固件、进程、gadget 配置

### 阶段二（09-06/07 上午）：多轮逆向
- **USBPcap 抓包（Windows 侧）**：模块在 Windows 11 即插即用（驱动自动 RADIO_POWER=1 开射频、配 APN），完全标准 MBIM 流程
- **ADB 进模块对比状态**：Windows 与 OpenFi 下模块内部 dmesg 完全一致（IPA uC 未加载不影响数据面）；数据面走 **GSI mbim DMA 直通**，`Ctrl Open=1` 是数据通道建立的关键标志
- **MBIM OPEN 消息格式解密**（决定性）：正确 OPEN = **16 字节**（12 字节头 type=1/len=16/txn + 4 字节 max_control_transfer，**无 fragment header**）；此前 python 脚本发 20 字节但 length 字段写 16（自身 bug）致模块拒 OPEN
- 期间发现模块 MBIM 实现的"病态"特征（详见 §5）

### 阶段三（09-07 晚）：数据面根因解密
- 结论一度定为"我们的 OPEN length 字段 bug + max_control_transfer 过大"（Windows 用 50）
- 修正版脚本 mbim_open_fix.py 就绪待测

### 阶段四（09-08 00:30）：最终根因（libmbim 版本）
- 本机（MM 1.24 + libmbim 1.32）全自动成功；OpenFi（MM 1.22 + libmbim 1.30）失败
- 定论：**libmbim 1.30 缺 "Fix re-opening a device when it was closed due to errors"（1.32 新增）**；模块会话残留后 1.30 无法 re-open
- 行动：把 OpenFi 的 libmbim 升到 1.32（OpenWrt snapshot apk 解包获取）

### 阶段五（09-08 01:45-02:00）：最终定界（内核 USB 栈）
- 裸 usbfs 控制传输测试（mbim_raw_open.c 交叉编译 aarch64 静态）绕开一切软件栈：
  - 本机（6.12 内核）：16B OPEN → **status=0 成功**
  - OpenFi（5.4 内核）：USB2/USB3、冷启动、清环境后仍 **status=2 拒绝**（txn 正确回显，证明消息被完整解析）
- **最终定论：模块无缺陷；MTK xhci-mtk + 内核 5.4.284 与工程固件 qbi 不兼容，软件栈无关**
- 旁证：高通 5G 模块在 MTK 平台不同内核版本上稳定性差异是社区共识（openwrt/openwrt#23002：T99W175 在 BPI-R4 内核 6.12 崩溃、6.6 正常）
- 候选方案排序：换 6.x 内核固件 → QMI 通道备用 → 模块留本机 → RNDIS 切换（红线，否）

### 阶段六（09-08 晚）：终极解法（OpenWrt 官方 6.18 内核）
- 用户指示"按 Debian 同款方案"：刷 OpenWrt 官方 snapshot（内核 6.18.44）+ ModemManager + libmbim 1.32
- **全自动成功**：MM 识别 → 5G 注册（CMCC 46000 / 5gnr / 54%）→ Bearer 双栈 → netifd 拨号 → LAN→5G 全链路通

### 阶段七（09-08 本次）：重启稳定性验证 + USB3 转移
- 模块物理转移到 USB3 口（用户操作）
- **发现并解决**：① 忘记装 SIM 卡导致 sim-missing（装卡后正常）；② 重启后 netifd 抢在 MM 就绪前失败且不重试；③ MM 不会自动 enable modem → 设计 hotplug 自动恢复链路（§7）
- 重启 5 轮验证，最终零人工干预全自动恢复

---

## 五、关键技术知识沉淀

### 5.1 MBIM 协议（本模块实测字节）
| 消息 | 字节数 | 结构 |
|---|---|---|
| OPEN | **16** | 12 头（type=1, len=16, txn）+ 4 字节 max_control_transfer；**无 fragment header** |
| COMMAND | 48 | 12 头 + 8 fragment（total=1, cur=0）+ 16 UUID + 4 CID + 4 cmdtype + 4 buflen |
| CLOSE | 12 | 12 头（type=2, len=12, txn） |

- libmbim 1.32 实际发送的 OPEN：`01 00 00 00 10 00 00 00 01 00 00 00 00 10 00 00`（maxctl=4096）
- 误加 fragment header 或 length 错误 → 模块回 OPEN_DONE **status=2** 或私有 0x80000007

### 5.2 模块 MBIM 实现的"病态"特征（工程固件 DF.004）
- **会话残留**：OPEN 后未干净 CLOSE 就退出 → 后续任何 OPEN 被拒（0x80000007 / status=2），干净 CLOSE（12B）可复位
- **COMMAND 响应流混乱**：标准 48B COMMAND 后收到 11 个私有 0x80000007 垃圾响应 + status=1 假响应——只有 libmbim 能过滤，裸 python 驱动不可行
- **平台敏感**：同一模块在 5.4 内核（MTK）上第一个 OPEN 即拒（qbi 启动即进入拒绝态）；6.12/6.18 内核正常

### 5.3 数据面路径（模块内部）
- USB → **GSI mbim（DMA 直通 modem）**，不依赖 IPA uC（IPA uC 未加载不影响数据面）
- 关键状态文件：`/sys/kernel/config/usb_gadget/g1/functions/gsi.mbim/info`；`Ctrl Open=1` 表示数据通道建立
- rmnet_data10 不参与数据面（此前相关排查全部是错误方向）

### 5.4 Windows 行为参照
- 即插即用核心：枚举后自动 RADIO_POWER=1（开射频）→ DEVICE_CAPS SET（APN=cmnet）→ CONNECT → 数据通
- Windows OPEN 52 字节（bulk 发送，额外数据被忽略；期望 20 字节，max_control_transfer=50）

---

## 六、最终实施方案（完整步骤，可复现）

### 6.1 刷机（OpenWrt 官方 6.18 内核）
1. 下载官方 snapshot：`openwrt-mediatek-filogic-openfi_6c-squashfs-sysupgrade.bin`（约 9.7MB）
2. **定制默认配置**（避免刷后失联）：解 sysupgrade tar（CONTROL/kernel/root 三件，无 fwtool hash 可直接重打）→ `unsquashfs root` → 注入 `/etc/uci-defaults/99-custom-lan`（LAN IP 改 192.168.21.1 匹配现网 + 设 root 密码）→ `mksquashfs`（xz, -b 262144）→ 重打 tar
3. `sysupgrade -n`（**不保留配置**——ImmortalWrt→OpenWrt 官方跨发行版，保留配置必出网络混乱/失联）
4. 刷后有线口 LAN=192.168.21.1 直接可控

### 6.2 驱动与软件栈（OpenWrt 25.x 模块化，apk 包管理）
```sh
# 内核驱动（官方快照为按需模块，必须手动装）
apk add kmod-usb-net-cdc-mbim kmod-usb-wdm kmod-usb-acm
# 自动拉依赖: kmod-usb-net-cdc-ncm kmod-usb-net-cdc-ether kmod-mii kmod-usb-net kmod-usb-core

# 软件栈（与 Debian 同版本）
apk add modemmanager libmbim mbim-utils
# 装后: ModemManager 1.24.0 / libmbim 1.32 / mbimcli 1.32
```
⚠️ 官方源下载易中断（wget exit 4）：本机 wget 下载 apk 后管道传 OpenFi，`apk add --allow-untrusted /tmp/xxx.apk`

### 6.3 端口识别修复（官方 MM 包缺 usbmisc hotplug 脚本）
自建 `/etc/hotplug.d/usbmisc/25-modemmanager-usbmisc`（官方只有 net/tty/wwan 三个脚本，cdc-wdm0 属 usbmisc 类，不补则 MM 收不到端口事件）：
```sh
#!/bin/sh
. /usr/share/ModemManager/modemmanager.common
[ -n "$DEVNAME" ] || exit
mkdir -m 0755 -p "${MODEMMANAGER_RUNDIR}"
mm_report_event "${ACTION}" "${DEVNAME}" "usbmisc" "/sys${DEVPATH}"
# 兜底: 设备添加后后台自动启用 modem (见 §7)
[ "${ACTION}" = "add" ] && [ -x /usr/lib/ModemManager/mm-autoconnect.sh ] && {
    /usr/lib/ModemManager/mm-autoconnect.sh >/dev/null 2>&1 &
}
```

### 6.4 WAN 接口配置（持久化）
```sh
uci set network.wwan=interface
uci set network.wwan.proto='modemmanager'
uci set network.wwan.device='0'      # ⚠️ 数字索引（见 §7.3 说明）
uci set network.wwan.apn='cmnet'     # 移动；联通 3gnet；电信 ctnet
uci set network.wwan.iptype='ipv4v6'
uci set network.wwan.auto='0'        # 关闭开机自启（由 hotplug 兜底拉起，见 §7）
uci commit network
uci add_list firewall.@zone[1].network='wwan'   # wan zone 加 wwan
uci commit firewall
```
- `device` 取值陷阱：`Modem/0` 短路径 mmcli 不认；完整 DBus 路径超限；**只有纯数字索引可用**（重启冷启动编号从 0 开始）

### 6.5 验收清单
```sh
mmcli -L                                   # 识别: [QCOM] SDXLEMUR-SD-MTP
mmcli -m 0                                 # state: connected / 5gnr / CMCC
ifstatus wwan                              # "up": true
ip route                                   # default via <gw> dev wwan0
ping -c 2 223.5.5.5                        # 0% loss
```

---

## 七、重启自动恢复机制（本次新增，已 5 轮验证）

### 7.1 发现的问题
1. **netifd 启动竞态**：OpenWrt 启动早期 netifd 即拉起 autostart 接口，此时 ModemManager 尚未在 DBus 就绪 → wwan 报 `couldn't find the ModemManager process in the bus` / `Device not managed by ModemManager` → 接口 down，**且不再重试**
2. **MM 不自动 enable modem**：MM 导出 modem 后停在 disabled（autoconnect 未触发），netifd 的 modemmanager proto 只负责 connect 不负责 enable → 拨号永远不会发生

### 7.2 解决方案
- wwan 接口 `auto=0`：避免 netifd 启动竞态失败
- `/usr/lib/ModemManager/mm-autoconnect.sh`（由 usbmisc hotplug 在 ACTION=add 时后台触发）：
  1. 等 ModemManager pid 文件（最多 60s）
  2. 等 15s（MM 初始化窗口）
  3. 轮询 mmcli -L 直到 modem 导出，`mmcli -m <n> --enable`（最多 14 次）
  4. `ifup wwan` 并轮询 ifstatus up（最多 8 次）

完整脚本 v4 内容：
```sh
#!/bin/sh
# ModemManager 自动启用兜底 + 拉起 wwan (v4)
wwan_up() { ifstatus wwan 2>/dev/null | grep -q "up\": true"; }
i=0
while [ $i -lt 60 ]; do
    [ -f /var/run/modemmanager/modemmanager.pid ] && break
    i=$((i + 1)); sleep 1
done
sleep 15
try=0
while [ $try -lt 14 ]; do
    num=$(mmcli -L 2>/dev/null | grep -oE "Modem/[0-9]+" | head -1 | grep -oE "[0-9]+$")
    if [ -n "$num" ]; then
        state=$(mmcli -m "$num" 2>/dev/null | grep -E "^[[:space:]]+state:" | head -1 | awk "{print \$2}")
        case "$state" in
            enabled|registered|connected|searching|connecting)
                logger -t "mm-autoconnect" "modem $num already $state"; break ;;
        esac
        if out=$(mmcli -m "$num" --enable 2>&1); then
            echo "$out" | grep -q "successfully enabled" && { logger -t "mm-autoconnect" "modem $num enabled OK"; break; }
        fi
        logger -t "mm-autoconnect" "enable attempt $try failed: $(echo $out | head -c 120)"
    else
        logger -t "mm-autoconnect" "attempt $try: modem not exported yet"
    fi
    try=$((try + 1)); sleep 4
done
try2=0
while [ $try2 -lt 8 ]; do
    wwan_up && { logger -t "mm-autoconnect" "wwan already up"; exit 0; }
    ifup wwan 2>/dev/null
    sleep 8
    wwan_up && { logger -t "mm-autoconnect" "wwan brought up OK"; exit 0; }
    try2=$((try2 + 1))
done
logger -t "mm-autoconnect" "wwan bring-up failed after 8 tries"; exit 1
```

### 7.3 验证结果（第 5 轮重启，零人工干预）
```
13:26:14 attempt 0: modem not exported yet
13:26:18 attempt 1: ...
13:26:34 modem 0 enabled OK
13:26:42 wwan brought up OK
```
→ 重启后约 90 秒：模块自动启用、5G 注册（connected/5gnr）、拨号、上网（0% 丢包，RTT ~20ms）

### 7.4 已知限制与注意
- **热插拔模块**（非重启）后 MM 编号递增（Modem/0 → Modem/1），`device='0'` 失配时 wwan 可能起不来 → **重启 OpenFi 复位**（冷启动编号归零）是最可靠手段
- 模块断电重启（拔插 USB）后 SIM 检测需要冷启动完成，MM 初始化约需 30-45 秒，属正常
- 兜底脚本日志：`logread | grep mm-autoconnect`

---

## 八、USB3 接口转移记录（本次）

| 项 | USB2 口（原） | USB3 口（现） |
|---|---|---|
| 拓扑 | usb1 根 → hub 1-1 → 1-1.3 | usb2 根 → hub 2-1 → 2-1.2 |
| 速度 | 480 Mbps (HighSpeed) | 5000 Mbps (SuperSpeed) |
| 驱动 | cdc_mbim / cdc-wdm0 / wwan0 | 同左，完全一致 |
| 结果 | 正常 | 正常（5G 注册、拨号、上网全通） |

- **转移经验**：① 转移前确认 SIM 卡已插入（本次因忘装 SIM 出现 sim-missing，装卡后重启即恢复）；② 转移后 MM 会重新发现模块（编号可能变化）；③ 物理移动模块本身安全（注意红线只针对 DIAG/ADB 口写数据）
- dmesg 特征（正常枚举）：`usb 2-1.2: new SuperSpeed USB device ... using xhci-mtk` → `cdc_mbim 2-1.2:1.2: cdc-wdm0: USB WDM device` → `wwan0: register 'cdc_mbim'`

---

## 九、日常运维手册

### 9.1 常用命令
```sh
mmcli -L                              # 列出模块
mmcli -m 0                            # 模块详情（状态/信号/运营商）
mmcli -m 0 --enable                   # 手动启用（兜底脚本失败时）
mmcli -m 0 --create-bearer="apn=cmnet,ip-type=ipv4v6" && mmcli -m 0 --connect=...  # 手工拨号
ifstatus wwan                         # 接口状态
logread | grep mm-autoconnect         # 自动恢复日志
tail -50 /var/log/mm.log              # MM 调试日志（LOG_LEVEL=DEBUG）
```

### 9.2 故障排查顺序
1. USB 枚举：`/sys/bus/usb/devices/2-1.2/idVendor` 应为 `05c6`
2. 驱动设备：`/dev/cdc-wdm0` 存在 + `ls /sys/class/net/ | grep wwan`
3. MM 识别：`mmcli -L` 有输出
4. 模块状态：`mmcli -m 0` state 应为 registered/connected
5. 接口/路由：ifstatus wwan up + default route via wwan0
6. 兜底日志：logread 查 mm-autoconnect
7. 顽固故障：**重启 OpenFi**（模块冷启动，一切归零）

### 9.3 已知问题
- sim-pin2 锁存在但不阻塞注册/拨号（固定拨号功能锁）
- MM 日志中 ICCID/IMSI 显示 '###'（USB3 口下 SIM 标识读取兼容性小问题，不影响业务）
- `mmcli -m 0 --enable` 偶报 `Couldn't get primary port` 警告但 enable 实际成功（MM 时序小 bug，无害）

---

## 十、历史报告索引（内容已并入本文档）

| 日期 | 文件 | 核心内容 |
|---|---|---|
| 08-29 | T99W368_OpenFi6C_排障全记录_20260829.md | USB 锁死事故、QMI 通道排查、模块内部确认 |
| 08-29 | T99W368_OpenFi6C_进度报告.md | 模块诊断、USB 模式切换测试、QMI 测试 |
| 08-29 | T99W368模块完整分析报告.md | 模块基本信息、USB 接口布局、Windows 对照 |
| 09-06 | T99W368_OpenFi6C_完整逆向进度_20260906.md | 逆向工程进度：USBPcap、MBIM 直连 |
| 09-06 | T99W368_第二轮回审_20260906.md | 误判纠正、PPP 方向、wwan0 盲点 |
| 09-06 | deepseek_markdown_20260906_2c8f76.md | QMI WDS Bind Mux Data Port 思路 |
| 09-07 | T99W368_OpenFi6C_最终逆向报告_20260907.md | 最终逆向：数据面因果链 |
| 09-07 | T99W368_数据面根因解密_20260907.md | GSI 数据面解密、OPEN length bug、Windows 序列 |
| 09-07 | T99W368_本机实测对比分析_20260907.md | 本机 vs OpenFi 差异分析 |
| 09-07 | T99W368_本机拔插基线_20260907.md | 本机拔插全流程基线、数据面假死现象 |
| 09-07 | T99W368_第三轮深度诊断_20260907.md | qbi 状态机、strace、QMUX 排除 |
| 09-07 | T99W368_第三轮_ModemManager上游研究_20260907.md | FCC 解锁、QMI-over-MBIM、上游源码 |
| 09-07 | T99W368_第四轮_DSI_QMUX根因收敛_20260907.md | QMUX 通道不可用、DSI 不匹配、完成度评估 |
| 09-08 | T99W368_最终根因_20260908.md | libmbim 1.30 缺 re-open（中期定论） |
| 09-08 | T99W368_最终定界_20260908.md | 5.4 内核 USB 栈定界（最终根因） |
| 09-08 | T99W368_OpenFi6C_最终解法_20260908.md | 6.18 内核方案成功报告（并入本文档扩充） |

---

## 十一、工具与物料清单

| 物料 | 位置 |
|---|---|
| 裸测试工具（usbfs 控制传输） | /tmp/fwcheck/mbim_raw_open.c（aarch64 静态交叉编译） |
| 定制固件（已刷入 OpenFi） | /tmp/fwcheck/openwrt-openfi_6c-custom-sysupgrade.bin |
| 旧 ImmortalWrt 配置备份 | /tmp/fwcheck/openfi-config-backup.tar.gz |
| 原厂 ImmortalWrt 固件 | /home/hong/下载/immortalwrt-24.10.5/ |
| 用户刷入的固件（已弃用） | /home/hong/桌面/openfi_6c-20260518_2250-immwrt-squashfs-sysupgra.bin |
| libmbim 1.32 ipk/apk 物料 | 本机 /tmp/fwcheck/ 及历史 /tmp 路径 |
| 自动恢复脚本（OpenFi 上） | /usr/lib/ModemManager/mm-autoconnect.sh |
| hotplug 脚本（OpenFi 上） | /etc/hotplug.d/usbmisc/25-modemmanager-usbmisc |

---

*本文档为 T99W368 × OpenFi6C 项目的唯一权威记录。历史 16 份报告已全部并入；如需原始文件，见 /home/hong/下载/T99W368历史归档_20260908.tar.gz*
